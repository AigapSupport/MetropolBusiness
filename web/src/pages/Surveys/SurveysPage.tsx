import PagePlaceholder from '../../components/PagePlaceholder';

/** PANELS_SPEC §A.6 — anket listesi, oluşturma, sonuçlar. */
export default function SurveysPage() {
  return <PagePlaceholder title="Anketler" />;
}
