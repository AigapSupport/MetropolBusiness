import PagePlaceholder from '../../components/PagePlaceholder';

/** PANELS_SPEC §A.5 — segment × modül yetki matrisi. */
export default function ModulePermissionsPage() {
  return <PagePlaceholder title="Modül Yetkileri" />;
}
