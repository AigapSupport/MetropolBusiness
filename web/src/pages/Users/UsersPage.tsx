import PagePlaceholder from '../../components/PagePlaceholder';

/** PANELS_SPEC §A.3 — kullanıcı listesi, filtreler, ekle/düzenle. */
export default function UsersPage() {
  return <PagePlaceholder title="Kullanıcılar" />;
}
