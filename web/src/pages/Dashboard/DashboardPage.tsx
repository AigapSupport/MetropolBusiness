import PagePlaceholder from '../../components/PagePlaceholder';

/** PANELS_SPEC §A.2 — KPI kartları, son hareketler, hızlı aksiyonlar. */
export default function DashboardPage() {
  return <PagePlaceholder title="Dashboard" />;
}
