import PagePlaceholder from '../../components/PagePlaceholder';

/** PANELS_SPEC §A.7 — duyuru listesi ve oluşturma/düzenleme. */
export default function AnnouncementsPage() {
  return <PagePlaceholder title="Duyurular" />;
}
