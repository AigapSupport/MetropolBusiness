import PagePlaceholder from '../../components/PagePlaceholder';

/** PANELS_SPEC §A.8 — video listesi, ekleme, izlenme raporu. */
export default function VideosPage() {
  return <PagePlaceholder title="Videolar" />;
}
