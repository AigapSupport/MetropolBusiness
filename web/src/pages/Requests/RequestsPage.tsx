import PagePlaceholder from '../../components/PagePlaceholder';

/** PANELS_SPEC §A.9 — izin & masraf talepleri (firma görünümü). */
export default function RequestsPage() {
  return <PagePlaceholder title="Talepler" />;
}
