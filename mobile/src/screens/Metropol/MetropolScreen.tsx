/** Metropol ana ekranı (PRD §8.1) — kart slider + bakiye + aksiyonlar Faz 1.6'da bağlanacak. */
import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export function MetropolScreen() {
  return <PlaceholderScreen titleKey="tabs.metropol" />;
}
