/**
 * Keşfet (harita) — Faz 2.1 kapsamında (TODO.md): MerchantList + harita + pin +
 * filtre barı. Harita native modül gerektirir; bu fazda placeholder gösterilir.
 * TODO(Faz 2.1): react-native-maps kurulumu + GET /metropol/merchants bağlanacak.
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export function ExploreScreen() {
  return <PlaceholderScreen titleKey="metropol.actions.explore" />;
}
