/** Diğer / İK modülleri (PRD §10) — yetkiye göre modül grid'i Faz 1.9'da bağlanacak. */
import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export function OtherScreen() {
  return <PlaceholderScreen titleKey="tabs.other" />;
}
