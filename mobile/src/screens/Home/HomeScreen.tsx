/** Ana Sayfa (PRD §6) — duyuru/anket/video akışı Faz 1.4'te bağlanacak. */
import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export function HomeScreen() {
  return <PlaceholderScreen titleKey="tabs.home" />;
}
