/** Sohbet (PRD §9) — sohbet listesi + SignalR entegrasyonu Faz 1.8'de bağlanacak. */
import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export function ChatScreen() {
  return <PlaceholderScreen titleKey="tabs.chat" />;
}
