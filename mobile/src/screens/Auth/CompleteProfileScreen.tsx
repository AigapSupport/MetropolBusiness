/** Profil tamamlama (PRD §5.1 — yeni kullanıcı: ad/soyad/e-posta) Faz 1.2'de gelecek. */
import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export function CompleteProfileScreen() {
  return <PlaceholderScreen titleKey="auth.completeProfileTitle" />;
}
