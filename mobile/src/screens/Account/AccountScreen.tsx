/** Hesabım (PRD §11) — hamburger menüden erişilecek; profil/ayarlar Faz 1.9'da bağlanacak. */
import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export function AccountScreen() {
  return <PlaceholderScreen titleKey="account.title" />;
}
